# Секреты и локальная БД — никогда в git
.env
.env.local
.env.*.local
backend/.env
backend/prisma/dev.db
backend/prisma/*.db-journal

# Зависимости
node_modules/
frontend/node_modules/
backend/node_modules/

# Сборка / логи
dist/
build/
*.log
.DS_Store

# Случайный lock в корне (зависимости — в frontend/ и backend/)
/package-lock.json

# Playwright (итерация 10)
test-results/
playwright-report/
blob-report/
